export default function generateFeelingPrompt() {
    return "complete the ‘feeling’ data object with the fields 'underlyingFeelings' (feeling words that do not imply blame), and 'needs' and their respective values. Return only the completed data object."
}
export default function generateFeelingPrompt() {
    return "complete the 'feeling' data object by adding an array of six relevant words to each of the fields 'underlyingFeelings' (feeling words that do not imply blame), and 'needs' and their respective values. Return only the completed data object."
}
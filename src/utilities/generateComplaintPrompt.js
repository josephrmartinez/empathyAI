export default function generateComplaintPrompt() {
    return "complete the 'complaint' data object by adding an array of six relevant words to each of the fields 'initialFeelings' (feeling words that end in -ed), 'underlyingFeelings' (feeling words that do not imply blame), and 'needs'. Return only the completed data object."
}